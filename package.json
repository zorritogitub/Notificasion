{
  "name": "countdown-notification",
  "version": "1.0.0",
  "description": "App de cuenta regresiva con notificaciones fijas",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp *.html *.json *.js www/ 2>/dev/null || true",
    "android": "npx cap add android && npx cap sync",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^5.0.0"
  }
}
